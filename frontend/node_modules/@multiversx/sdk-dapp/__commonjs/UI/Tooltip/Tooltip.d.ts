import { PropsWithChildren, ReactNode } from 'react';
import { WithStylesImportType } from 'hocs/useStyles';
import { WithClassnameType } from 'UI/types';
export interface TooltipPropsType extends WithStylesImportType, PropsWithChildren, WithClassnameType {
    trigger: (isTooltipVisible: boolean) => ReactNode;
    triggerOnClick?: boolean;
    position?: 'top' | 'bottom';
}
export declare const Tooltip: (props: TooltipPropsType) => JSX.Element;
//# sourceMappingURL=Tooltip.d.ts.map