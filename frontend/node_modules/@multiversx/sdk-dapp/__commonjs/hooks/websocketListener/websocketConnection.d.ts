import { Socket } from 'socket.io-client';
export declare enum WebsocketConnectionStatusEnum {
    NOT_INITIALIZED = "NOT_INITIALIZED",
    PENDING = "PENDING",
    COMPLETED = "COMPLETED"
}
declare type StatusListenerType = (status: WebsocketConnectionStatusEnum) => void;
declare class WebsocketConnection {
    current: Socket | null;
    private _status;
    private listeners;
    get status(): WebsocketConnectionStatusEnum;
    set status(newStatus: WebsocketConnectionStatusEnum);
    subscribe(listener: StatusListenerType): () => void;
    unsubscribe(listener: StatusListenerType): void;
    private notifyListeners;
}
export declare const websocketConnection: WebsocketConnection;
export {};
//# sourceMappingURL=websocketConnection.d.ts.map